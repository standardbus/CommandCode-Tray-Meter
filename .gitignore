# Local credentials: never commit these
config.json
.env
.env.*
*.pem
*.key
.credentials.yaml

# Runtime state
.cache/
*.log

# Build artefacts: scripts/build-exe.ps1 regenerates them
CommandCodeMonitor.exe
csharp/app.ico

# Screenshots holding private data (desktop, chat, session): never publish
screenshots/exe-bubble.png
screenshots/exe-bubble-on-screen.png
